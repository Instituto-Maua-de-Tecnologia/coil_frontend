export default interface IProjectCatalog {
    projects: [
        {
            title: string;
            logo: string;
            type_activity: number;
        }
    ];
    mobilities: [
        {
            title: string;
            logo: string;
            type_activity: number;
        }
    ];
}
